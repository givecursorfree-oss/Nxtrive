{
  "name": "nxtrive-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Nxtrive marketing site — Agentflow (Home V1) template converted to React, rebranded to Nxtrive.",
  "scripts": {
    "gen": "node build-template.mjs",
    "seo": "node scripts/seo-apply.mjs",
    "dev": "npm run gen && npm run seo && vite",
    "build": "npm run gen && npm run seo && vite build && node scripts/seo-apply.mjs --prerender",
    "preview": "vite preview"
  },
  "dependencies": {
    "html-react-parser": "^5.2.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
