# Canonical production URL (no trailing slash). Used for canonical tags, Open Graph, sitemap, and JSON-LD.
# Temporary Vercel URL until nxtrive.app DNS is connected
VITE_SITE_URL=https://nxtrive.vercel.app
